# Generated API contract

`openapi.json` and `schema.ts` are generated from the FastAPI application.
Do not edit them by hand. From an activated `MyLabData` environment, run:

```powershell
npm run openapi:generate
```

Handwritten API code belongs in the parent `src/api` directory.
